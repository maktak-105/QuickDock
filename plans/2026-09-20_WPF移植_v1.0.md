# 2026-09-20 QuickDock WPF 移植 計画 v1.0

## 選定

- WPF / .NET 10（安定最新）。Explorer の FileDrop が本線。
- Rust+WebView2 は常駐 64px ドックに重い。Explorer OLE と HTML5 D&D が別物。

## 今回

1. `Microsoft.DotNet.SDK.10` を入れる
2. `src/app` を WPF に差し替え。`AllowDrop`、ドロップ中はサイズ固定
3. `scripts/build.bat` は `dotnet publish`（win-x64 単体 exe）
4. `proto/selftest.ps1` で登録・展開・起動を確認
