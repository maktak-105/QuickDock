# QuickDock Windows起動時実行設定 結果

## 実施内容
- ドック本体とトレイの右クリックメニューに「Windows起動時に実行」を追加。
- 現在ユーザーのRunキーに現在のQuickDock実行パスを登録・解除。チェック状態はメニュー表示時に再読込。
- 設定エラー時はチェックを戻してメッセージを表示。
- 英日README・仕様書・変更履歴を同期。

## 検証
- `dotnet build src/app/QuickDock.csproj --configuration Release --nologo` 成功（0エラー）。
- 既存の警告 `WFO0003`（manifestの高DPI設定）1件。今回の変更箇所とは無関係。
- `git diff --check` 成功。
- 起動時登録・解除の実機操作は未実施。
